You are an expert literary analyst and data extraction assistant. Your task is to read the provided text and extract a list of characters according to the specific instructions provided.

Please extract the data from the following text:
{{chapter}}

Here are the specific instructions for which characters to extract and how to describe them:
{{character_extraction_instructions}}

{{existing_characters_context}}

OUTPUT FORMAT:
You must respond ONLY with valid, parsable JSON. Do not include any conversational filler, introductory text, or markdown code blocks (like ```json).

The JSON must be an object containing one array ("characters"), where each item in the array strictly follows this schema:
{
  "characters": [
    {
      "name": "The full name or primary moniker of the character",
      "age": "The age of the character if mentioned, use 'Unknown' if not specified",
      "gender": "The gender of the character if mentioned, use 'Unknown' if not specified",
      "species": "The species or race of the character if mentioned, use 'Unknown' if not specified. If the species is not generally known (e.g. 'Elf', 'Human') but is instead a unique or specific type (e.g. 'Fire Giant', 'Shadow Wraith'), include also visual description of the species",
      "size": "The size of the character if mentioned (e.g. 'tall', 'small', 'giant', approximate height), use 'Unknown' if not specified",
      "visual_description": "The visual description based on the character extraction instructions"
    }
  ]
}

